# bumpver.toml in project root

[bumpver]
current_version = "2023.2.2"
version_pattern = "YYYY.MM.INC0[-TAG]"
commit_message = "bump: {new_version}"
commit = true
tag = false
push = false

[bumpver.file_patterns]
"package.json" = [
    '"version": "{version}"',
]
"bumpver.toml" = [
    'current_version = "{version}"',
]